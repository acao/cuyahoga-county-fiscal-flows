# Python dependencies for markdown to HTML and PDF conversion
# Required for GitHub Actions publishing workflow

# Core markdown processing
markdown>=3.5.0

# PDF generation
weasyprint>=60.0

# HTML processing and parsing
beautifulsoup4>=4.12.0

# Template engine
jinja2>=3.1.0

# Additional markdown extensions (optional)
pymdown-extensions>=10.0.0

# Chart extension dependencies
pyyaml>=6.0

# PDF chart capture dependencies
playwright>=1.40.0